{
  "name": "@ai-repo-maintainer/action",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "bundle": "ncc build src/index.ts -o dist --source-map --minify",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "@ai-repo-maintainer/core": "workspace:*"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
